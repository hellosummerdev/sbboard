<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sbboard.comment.CommentMapper">
    <insert id="create" parameterType="commentDto">
        insert into comment(board_seq, comment_content, user_id)
        values (#{board_seq}, #{comment_content}, #{user_id})
    </insert>

    <select id="commentList" resultType="commentDto">
        select *
        from comment
        where use_yn = 1
        order by created_at desc
    </select>
</mapper>